project('constexpr-string', 'cpp',
  version : '0.1',
  default_options : ['warning_level=3',
                     'cpp_std=c++17'])

exe = executable('constexpr_string', 'main.cpp',
  install : true)

test('basic', exe)
